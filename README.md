# aws-cdk-go-template
Template for AWS CDK bindings for Go.
## Setup
```shell
npm i aws-cdk
```
```shell
mkdir rds
cd rds
```
```shell
# Must run in a empty directory!
npx cdk init app --language go
```
